{
  "name": "aws-cors-api",
  "version": "1.0.0",
  "description": "AWS REST API with CORS",
  "main": "index.js",
  "scripts": {
    "build": "gulp build",
    "deploy": "gulp deploy && gulp deploy:describe"
  },
  "author": "Jeff Cooper",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-shell": "^0.6.3"
  }
}
